package dev.wisespirit.toco_cloud;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.GetMapping;
/**
 * this controller move to config class because of provide simplicity
 * this controller's work no much
 * see WebConfig class in config folder
 */
/*

@Controller
public class HomeController {
    @GetMapping("/")
    public String home(){
        return "home";
    }
}
*/
